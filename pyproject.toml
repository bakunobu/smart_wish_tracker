# pyproject.toml

[tool.black]
line-length = 88

[tool.flake8]
max-line-length = 88
extend-ignore = ["E203", "W503"]  # Optional: compatible with Black
select = ["E", "W", "F"]          # Only check syntax, errors, flakes